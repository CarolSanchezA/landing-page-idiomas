export class PainPoint {
  constructor(id, title, description, icon) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.icon = icon; // Aqui guardaremos o nome do ícone do MUI ou o componente
  }
}